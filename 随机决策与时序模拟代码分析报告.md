# 随机决策与时序模拟代码分析报告

> 分析日期：2026-06-27  
> 分析范围：`dy/` 目录下所有 Python 源文件

---

## 一、总览

| 维度 | 数量 |
|------|------|
| **涉及文件数** | 7 |
| **random.uniform 调用** | 42 处 |
| **random.randint 调用** | 31 处 |
| **random.random 概率决策** | 6 处 |
| **random.choice 调用** | 4 处 |
| **time.sleep 调用** | 28 处 |
| **human_sleep 调用** | 26 处 |
| **human_swipe / human_click 随机化** | 18 处 |
| **合计随机化代码点** | **155+** |

---

## 二、按文件分类统计

### 2.1 `dy/anti_detection.py` — 防风控核心引擎（71 处）

这是最大的随机化浓度文件，承担整个系统的行为拟人化。

#### A. 贝塞尔曲线滑动模拟（9 处 `random`）

| 行号 | 类型 | 用途 |
|------|------|------|
| 70 | `uniform(0.08, 0.2)` | 贝塞尔控制点偏移幅度 |
| 71 | `uniform(-π/3, π/3)` | 偏移角度随机化 |
| 72-75 | `uniform(0.3~1.0)` ×4 | 两个控制点坐标随机化 |
| 84-85 | `uniform(-1.5, 1.5)` ×2 | 轨迹点微抖动（模拟手指不稳） |

#### B. 滑动速度随机化（6 处 `random`）

| 行号 | 类型 | 用途 |
|------|------|------|
| 101 | `uniform(0.10, 0.18)` | 长距离滑动 duration |
| 103 | `uniform(0.06, 0.12)` | 短距离滑动 duration |
| 106 | `randint(5, 15)` | 起止点抖动幅度 |
| 107-110 | `randint(-jitter, jitter)` ×4 | 起止点坐标随机偏移 |
| 127 | `uniform(0.20~0.55)` | 曲线滑动 duration |
| 129 | `randint(3, 12)` | 曲线起止抖动 |
| 130-133 | `randint(-jitter, jitter)` ×4 | 曲线起止坐标偏移 |

#### C. 人性化点击随机化（9 处 `random`）

| 行号 | 类型 | 用途 |
|------|------|------|
| 176-177 | `randint(-8, 8)` ×2 | 单击坐标抖动 ±8px |
| 186-187 | `randint(-10, 10)` ×2 | 双击第1次坐标抖动 |
| 191 | `uniform(0.08, 0.18)` | 双击间隔时间 |
| 194-195 | `randint(-10, 10)` ×2 | 双击第2次坐标抖动 |

#### D. 人性化等待系统（5 处 `random` + 6 个预定义区间）

| 行号 | 类型 | 描述 |
|------|------|------|
| 209-218 | `SLEEP_RANGES` 字典 | 6 个预定义场景：「fast」「normal」「slow」「page_load」「video_stay」「comment_browse」 |
| 230 | `uniform(min, max)` | 基于场景取随机等待 |
| 233-234 | `random() < 0.05` + `uniform(2, 6)` | **5% 概率模拟走神**，额外加 2~6 秒 |
| 249 | `uniform(-0.1, 0.2)` | 可中断步进间隔随机化 |

#### E. 概率化互动决策（6 处 `random` + 6 个默认概率）

| 行号 | 类型 | 描述 |
|------|------|------|
| 263-270 | `DEFAULT_PROBABILITIES` | 6 个默认概率：like=45%, comment=15%, follow=20%, pm=10%, comment_lead=30%, long_watch=15% |
| 283 | `random() < probability` | 每次互动掷骰决定是否执行 |

#### F. 夜间静默 — 随机轮询（1 处 `random`）

| 行号 | 类型 | 描述 |
|------|------|------|
| 422 | `uniform(25, 40)` | 夜间等待轮询随机化（不固定 30s） |

#### G. 行为模式随机化（14 处 `random`）

| 行号 | 类型 | 功能 | 概率 |
|------|------|------|------|
| 541 | `random() < 0.05` | **回看上一个视频** | 5% |
| 546-555 | `randint(-20,20)` ×4 | 回看滑动坐标抖动 | — |
| 564 | `random() < 0.08` | **快速划过视频** | 8% |
| 568-570 | `randint(-15,15)` ×2 | 快速划过坐标抖动 | — |
| 572 | `uniform(0.15, 0.25)` | 快速划过时间 | — |
| 582 | `random() < 0.03` | **切到首页推荐流浏览** | 3% |
| 597 | `randint(2, 4)` | 推荐流刷 2~4 个视频 | — |
| 600-602 | `randint(-20,20)` ×2 | 首页浏览滑动坐标 | — |
| 614 | `random() < 0.04` | **长停顿模拟走神** | 4% |
| 615 | `uniform(5, 15)` | 走神停顿 5~15 秒 | — |

---

### 2.2 `dy/actions/commenting.py` — 评论操作（11 处 `random`）

| 行号 | 类型 | 描述 |
|------|------|------|
| 51 | `time.sleep(0.3)` | 查找输入框轮询间隔（固定） |
| 85 | `time.sleep(0.3)` | 验证消息可见轮询（固定） |
| 101 | `time.sleep(0.3)` | 再次验证轮询（固定） |
| 107 | `random.uniform(0.15, 0.3)` | 输入框点击后等待 |
| 154 | `random.uniform(0.5, 1.0)` | 关闭输入框后返回等待 |
| 173 | `random.uniform(0.5, 1.2)` | 输入完成后到点击发送的间隔 |
| 180 | `random.uniform(0.8, 1.5)` | 发送后等验证的间隔 |
| 198 | `random.uniform(0.5, 1.2)` | 重试发送等待 |
| 203 | `random.uniform(1.0, 2.0)` | 回车发送后等待 |
| 336 | `random.uniform(1.0, 2.0)` | 楼中楼回复发送后等待 |
| 427-435 | `randint(-5,5)` ×4 + `randint(-10,10)` ×2 | 评论区滑动坐标随机化 |

---

### 2.3 `dy/actions/interaction.py` — 互动操作（8 处 `random`）

| 行号 | 类型 | 描述 |
|------|------|------|
| 34 | `uniform(0.15, 0.3)` | 点击等待 |
| 113-116 | `randint(-10,10)` ×4 | 左侧滑动返回坐标随机化 |
| 117 | `uniform(0.10, 0.16)` | 滑动 duration 随机化 |
| 192 | `random.choice(message_list)` | **私信话术随机轮播** |
| 464 | `uniform(0.8, 1.5)` | 关注操作后等待 |
| 687 | `uniform(1.5, 2.5)` | 主流程等待 |

---

### 2.4 `dy/actions/navigation.py` — 导航操作（0 处直接 `random`，26 处 `human_sleep`）

此文件不直接调用 `random`，但通过 `human_sleep` 间接实现随机化：

| 调用类型 | 次数 | 间接随机范围 |
|---------|------|------------|
| `human_sleep('fast')` | 10 | 0.3~0.8s (fast 区间) |
| `human_sleep('normal')` | 13 | 0.8~2.0s (normal 区间) |
| `human_sleep('page_load')` | 2 | 2.5~5.0s (page_load 区间) |

---

### 2.5 `dy/task_runner.py` — 任务调度（4 处 `random` + 多处间接随机）

| 行号 | 类型 | 描述 |
|------|------|------|
| 70 | `uniform(8, 15)` | 弱网信号等待 8~15s |
| 138 | `uniform(0.3, 0.8)` | 可中断休眠步进随机化 |
| 693 | `uniform(min_stay, max_stay)` | **视频停留间隔随机化**（最小值 max(5, 配置)） |
| 697 | `uniform(5.0, 15.0)` | 长停留模式（15% 概率）额外随机停留 |

间接随机：
- `_interruptible_sleep` 每步：`uniform(0.3, 0.8)` 始终随机
- human_swipe / human_click 通过 `self.anti.gesture` 调用（见 anti_detection 统计）
- human_double_click 通过 `self.anti.human_double_click` 调用
- `BehaviorRandomizer` 四个随机行为（见 anti_detection 统计）

---

### 2.6 `dy/pipeline.py` — 管道引擎（1 处 `random`）

| 行号 | 类型 | 描述 |
|------|------|------|
| 141 | `uniform(1.0, 2.5)` | 步骤间随机间隔 |

---

### 2.7 `dy/ai_reply_agent.py` — AI 回复代理（3 处 `random`）

| 行号 | 类型 | 描述 |
|------|------|------|
| 441 | `random.choice(question_templates)` | 问价类截流回复随机选模板 |
| 443 | `random.choice(tutorial_templates)` | 教程类截流回复随机选模板 |
| 444 | `random.choice(generic_templates)` | 通用截流回复随机选模板 |

---

## 三、按功能维度分类

### A. 随机决策系统（Probability-based Decisions）

| 决策点 | 概率 | 文件 | 说明 |
|--------|------|------|------|
| 点赞决策 | **45%** | anti_detection.py:264 | 按概率决定是否点赞 |
| 评论决策 | **15%** | anti_detection.py:265 | 按概率决定是否评论 |
| 关注决策 | **20%** | anti_detection.py:266 | 按概率决定是否关注 |
| 私信决策 | **10%** | anti_detection.py:267 | 按概率决定是否私信 |
| 查看评论区 | **30%** | anti_detection.py:268 | 按概率决定是否查看评论区 |
| 长停留模式 | **15%** | anti_detection.py:698 | 按概率延长视频停留 5~15s |
| 走神停顿 | **5%** | anti_detection.py:233 | 操作间隙额外停顿 2~6s |
| 回看视频 | **5%** | anti_detection.py:541 | 模拟回看上一个视频 |
| 快速划过 | **8%** | anti_detection.py:564 | 快速浏览不感兴趣内容 |
| 首页推荐流 | **3%** | anti_detection.py:582 | 切到首页浏览 2~4 个视频 |
| 长停顿走神 | **4%** | anti_detection.py:614 | 随机 5~15s 停顿 |

**总计 11 个概率化决策点**，覆盖所有互动行为和额外随机行为。

---

### B. 时间随机化（Timing Randomization）

| 场景 | 随机范围 | 文件 | 说明 |
|------|---------|------|------|
| 快速操作后等待 | 0.3~0.8s | anti_detection.py | `HumanSleep.sleep('fast')` |
| 普通操作后等待 | 0.8~2.0s | anti_detection.py | `HumanSleep.sleep('normal')` |
| 页面加载等待 | 2.5~5.0s | anti_detection.py | `HumanSleep.sleep('page_load')` |
| 视频停留 | 5~12s+ | task_runner.py:693 | `uniform(min_stay, max_stay)` |
| 长停留 | +5~15s | task_runner.py:697 | 15% 概率额外时间 |
| 步骤间隔 | 1.0~2.5s | pipeline.py:141 | 管道步骤间 |
| 弱网等待 | 8~15s | task_runner.py:70 | 4G 弱网场景 |
| 点击-输入间隔 | 0.15~0.3s | commenting.py:107 | 输入框激活后 |
| 输入-发送间隔 | 0.5~1.2s | commenting.py:173 | 输入完成到点击发送 |
| 发送后验证间隔 | 0.8~1.5s | commenting.py:180 | 发送后等 UI 更新 |
| 双击间隔 | 0.08~0.18s | anti_detection.py:191 | 双击两次点击间 |
| 夜间轮询间隔 | 25~40s | anti_detection.py:422 | 静默时段轮询 |

**总计 12 个时间随机化场景**。

---

### C. 空间随机化（Spatial Randomization）

| 场景 | 抖动范围 | 文件 | 说明 |
|------|---------|------|------|
| 单击坐标抖动 | ±8px | anti_detection.py:176 | 人性化点击 |
| 双击坐标抖动 | ±10px | anti_detection.py:186 | 两次点击不同位置 |
| 滑动起止点 | ±5~15px | anti_detection.py:106-110 | 普通滑动 |
| 曲线滑动起止点 | ±3~12px | anti_detection.py:129-133 | 贝塞尔曲线滑动 |
| 轨迹点微抖动 | ±1.5px | anti_detection.py:84-85 | 模拟手指不稳 |
| 贝塞尔控制点偏移 | 距离×8~20% | anti_detection.py:70-75 | 弧线形状随机 |
| 评论区滑动坐标 | ±5~10px | commenting.py:427-435 | 评论区内容滑动 |
| 互动操作坐标 | ±10px | interaction.py:113-116 | 返回滑动等 |

**总计 8 类空间随机化**。

---

### D. 内容随机化（Content Randomization）

| 场景 | 随机方式 | 文件 | 说明 |
|------|---------|------|------|
| 私信话术 | `random.choice(list)` | interaction.py:192 | 话术词库轮播 |
| 截流回复模板 | `random.choice(list)` | ai_reply_agent.py:441-444 | 3 类模板各 3 条，共 9 条随机选 |

---

## 四、随机化密度热力图

```
文件                       random   time.sleep   human_sleep   密度评级
──────────────────────────────────────────────────────────────────
anti_detection.py            71        5            1           ★★★★★  极高
commenting.py                11       10            1           ★★★★   高
interaction.py                8        5            1           ★★★★   高
navigation.py                 0        0           26           ★★★     中（全间接）
task_runner.py                4        4            9           ★★★     中
pipeline.py                   1        0            0           ★       低
ai_reply_agent.py             3        0            0           ★       低
```

---

## 五、关键发现

### 1. 随机化体系完整但集中度高

71/155（46%）的随机化集中在 `anti_detection.py`，这种集中设计便于维护和调参，是合理的架构。

### 2. 概率决策有"总开关"

`task_runner.py:101-105` 的 `_should_interact()` 提供了概率总开关（`enable_anti_detection_probability`），关闭后所有概率决策变为确定性执行。但注意 `BehaviorRandomizer` 的 4 个随机行为（回看、快速划过、首页浏览、走神）**不受此开关控制**，它们是 `_video_loop` 中直接调用的。

### 3. 固定与随机的混合

部分时间等待是固定值（如 `time.sleep(0.3)` 在评论轮询中），而大部分操作后等待是随机区间。这种混合设计合理——轮询用固定值确保响应性，操作间隔用随机值模拟人性。

### 4. 视频停留时间有下限保护

`task_runner.py:692`：`min_stay = max(5, config)`，最低 5 秒，避免配置过低产生机器特征。

---

## 六、可调优参数汇总

| 参数 | 默认值 | 位置 | 影响 |
|------|--------|------|------|
| 点赞概率 | 45% | anti_detection.py:264 | 控制点赞密度 |
| 评论概率 | 15% | anti_detection.py:265 | 控制评论密度 |
| 关注概率 | 20% | anti_detection.py:266 | 控制关注密度 |
| 私信概率 | 10% | anti_detection.py:267 | 控制私信密度 |
| 评论区查看概率 | 30% | anti_detection.py:268 | 控制截流触发率 |
| 长停留概率 | 15% | anti_detection.py:270 | 控制深度观看 |
| 走神概率 | 5% | anti_detection.py:233 | 操作间隙停顿 |
| 回看概率 | 5% | anti_detection.py:541 | 回看行为 |
| 快速划过概率 | 8% | anti_detection.py:564 | 跳过不感兴趣 |
| 首页浏览概率 | 3% | anti_detection.py:582 | 切推荐流 |
| 长停顿走神概率 | 4% | anti_detection.py:614 | 随机思考停顿 |
| 视频最短停留 | 5s | task_runner.py:692 | 防止过快特征 |
