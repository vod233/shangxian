# 2026-06-27

## 双模式架构 Code Review + 修复
- 对5个文件的改动做了完整 code review（schemas, main, douyin_app, task_runner, anti_detection）
- 发现问题：Streamlit 前端进程未重启导致浏览器显示旧 5-checkbox UI → 已 kill 旧进程 + 清 cache + 重启
- 修复3个🟡建议项：
  1. `_selected_feature_chain_enabled` 改用新模式字段 → dy/task_runner.py
  2. 后端新增双模式全关校验 → backend/main.py
  3. `ProcessCommentSectionAction` 改造为返回 `(closed, comment_made)` 元组，截流阶段 `record_action('comment')` 改为条件调用 → dy/actions/commenting.py + dy/task_runner.py
- 后端 API 重启后验证通过：全关配置被拒绝(409→改为success:false)，正常配置保存成功

## 长按菜单误触发 Bug 修复
- 根因分析：双根因
  1. `human_swipe_with_curve` 的 `max(20, ...)` 把所有曲线滑锁死在 0.8s（超 Android 500ms 长按阈值）
  2. `fast_tap` 的 `touch.up` 静默失败产生僵尸触控
- 修复 A：step_ms 下限 20→10ms + 硬上限 400ms + fallback swipe 也钳制 ≤400ms → dy/anti_detection.py
- 修复 B：fast_tap except 分支先 touch.up 收尸再 click 兜底 → dy/anti_detection.py
- 修复 C：_recover_to_video_page 入口主动 touch.up(0,0) 清理残留 → dy/task_runner.py
- 6 文件 py_compile ✅ | 6 步仿真场景全 ≤400ms ✅ | fallback 路径全 ≤400ms ✅ | API + 前端 ✅

## Phase 1: 管道化架构落地
- 新增 dy/pipeline.py (216行): PipelineStep + PipelineExecutor + 4个step函数 + PIPELINE_STEPS注册表
- 重构 dy/task_runner.py: _video_loop 互动段从 ~40行硬编码 → 6行 pipeline.run()
- 删除 _execute_mode_customer_acquisition (34行) 和 _execute_mode_content_interaction (43行)
- 清理未使用 import: DoubleClickLikeAction, FollowAuthorAction, PostCommentAction
- 10 项集成测试: step注册✓ 顺序✓ 属性✓ 阻断传播✓ 限额跳过✓
- task_runner.py: 796行 → 700行 (-96行)
- 改动范围: 1增1改, 0个Action改动, 0个frontend/backend改动

## 管道链路真机验证
- 设备: V4DUT20422013309 (USB)
- 新增 test_pipeline_chain.py: 5 项验证
  - 管道结构: 4步骤顺序正确 ✅
  - 调度逻辑: 全开/模式1关闭/模式1阻断 3场景 ✅
  - 步骤函数: 4/4 可导入 ✅
  - 集成: PipelineExecutor + TikTokTaskFlow 接口兼容 ✅
  - Flow属性: self.pipeline 存在于 __init__ ✅

## Phase 2: 前端按步骤粒度开关 + 预设模板
- dy/pipeline.py: PipelineStep 加 description/category/default_enabled; PipelineExecutor 支持 per-step config (优先于 mode-level); get_pipeline_step_info() 元数据导出
- backend/schemas.py: AppConfig 加 pipeline_steps: Dict[str,bool]
- backend/main.py: _infer_pipeline_steps() 向后兼容推断; GET /api/pipeline/steps 新端点; _save 持久化 pipeline_steps
- frontend/douyin/douyin_app.py: render_execution_functions 重写为步骤级复选框 + 分类分组 + 4个预设(全功能/仅获客/仅互动/仅点赞); 新增 _fetch_pipeline_steps + _save_config 工具函数
- 全栈验证: API /pipeline/steps 返回4步+enabled状态 ✅; /api/config pipeline_steps 字段 ✅; 5项管道链路测试全部通过 ✅
- 后端 PID 33220 (reload), 前端 streamlit 8501 端口

## 三大功能链路测试（私域获客 / AI视频评论 / 评论区截流）
- 代码静态审查 + 52项单元测试（50通过/2失败）→ test_3features_analysis.py
- 测试覆盖：7种单独链路场景 + 7种组合场景 + 边界条件 + AI代理 + 评论区操作 + 互动操作
- 发现13个问题：P0×3（双重恢复包裹/配额虚耗/意向识别遗漏）、P1×5（阻断过激/向后兼容推断/返回值语义/数据库脏写/解构冗余）、P2×5（间隔计算/配额竞态/阈值不一致/话术模板化/截断）
- 输出报告：三大功能链路测试报告.md

## 全部修复 + 评论发送验证增强
- 修改5个文件，52/52测试通过：
  1. dy/pipeline.py: PipelineStep+self_managed 标记（P0-1），_should_record_quota 精准配额记录（P0-2），步骤间隔预计算（P2-1），_step_acquisition stable 语义统一（P1-3），_step_video_comment 空值不写库（P1-4），_step_comment_lead 详细日志
  2. dy/actions/commenting.py: 新增 _input_is_cleared 函数（发送最强验证），_send_text_from_focused_input 三级验证链（清空→可见→消失），_is_reviewable_comment 阈值统一为3（P2-3）
  3. dy/ai_reply_agent.py: _local_intent_guess 扩展至30+常见意向词（P0-3），_build_lead_fallback 随机化多样性（P2-4），_sanitize_reply 智能截断（P2-5），import random
  4. dy/task_runner.py: _interaction_enabled 向后兼容推断精确化（P1-2），_run_comment_lead_safely 保留 closed_ok（P1-5）
  5. dy/anti_detection.py: DailyLimitManager 加 _lock 线程锁（P2-2）
- 评论发送验证核心改进：输入框清空检查 > 评论可见检查 > 文本消失检查，三级验证链确保"已编辑必发送"

## 随机决策与时序模拟代码分析
- 统计 7 个文件共 155+ 处随机化代码点
- anti_detection.py 占 71 处（46%），是整个系统随机化的核心
- 分类：11 个概率决策点 + 12 个时间随机化场景 + 8 类空间随机化 + 2 种内容随机化
- 输出报告：随机决策与时序模拟代码分析报告.md

## 消除全部概率决策 + 时间随机化（内部测试模式）
- 修改 6 个文件，52/52 测试通过：
  1. dy/anti_detection.py: HumanSleep 6个区间→固定中点 + 移除5%走神; InteractionProbability 全部→1.0(长停留→0); BehaviorRandomizer 4方法→空操作; 双击间隔→0.1s; 夜间轮询→30s
  2. dy/actions/commenting.py: 6处 random.uniform→固定中点; 4处时间random→固定值
  3. dy/actions/interaction.py: 3处 random.uniform→固定中点; random.choice→首条
  4. dy/task_runner.py: 视频停留→中点; 弱网等待→11.5s; _interruptible_sleep步进→0.5s; 长停留移除
  5. dy/pipeline.py: 步骤间隔→1.75s
  6. dy/ai_reply_agent.py: 3处 random.choice→首条模板
- 空间随机化保留（贝塞尔控制点/坐标抖动），不影响行为可预测性

## Review 修复：消除残余时间random + 值精度
- pipeline.py: 1.5→1.75s（修正中点偏差14%），移除 import random
- task_runner.py: 10→11.5s（修正中点偏差13%）
- anti_detection.py: human_swipe 滑动duration 2处→中点0.14s/0.09s，human_swipe_with_curve→0.42s/0.30s
- interaction.py: human_swipe_curve duration 0.13s
- ai_reply_agent.py: 移除未使用的 import random
- 52/52 测试通过

## AI员工全功能链路详解文档
- 按全开模式（4个管道步骤全部勾选）逐函数追踪完整执行流程
- 覆盖: start→launch_app→搜索→筛选→进入视频流→视频循环→4步管道→滑动→停留
- 详细说明每个 Action.execute() 的内部实现、策略方法、返回值语义
- 附录: 8种页面状态识别矩阵、三级评论验证链、日限额体系、阻断链规则
- 输出: AI员工全功能链路详解.md

## 长按弹窗根因修复
- 排除贝塞尔曲线（≤400ms，安全），定位到 fast_tap touch.up 静默失败导致僵尸触控累积
- 修复: fast_tap 加 finally 块无条件 touch.up(0,0) 兜底清理（26处调用点全覆盖）

## 长按弹窗终极修复（方案A+B — 19处根本性替换）
- 方案B: fast_tap 改为 down→微移+1px→up 三段式，从根上消除Android长按判定
- 方案A: 新增 BaseAction.safe_node_click()，4个action文件中 19处 node.click()/d.click() 统一替换为 fast_tap/safe_node_click/human_click
- navigation.py:7 / interaction.py:9 / commenting.py:8 / base_action.py:+safe_node_click
- 5文件编译通过, 52/52测试通过

## PostCommentAction 视频页发送策略优化
- 问题: 之前 hint 路径会触发"关闭"返回导致视频页破坏，且路径2兜底会直接return False
- 修复: 3条路径(已存输入框→hint触发→坐标兜底)全部走完，输入失败不直接放弃
- 关键: 发完只关闭键盘不关评论区，交给外层 _recover_to_video_page 兜底
- 52/52测试通过

## 对标参考项目重构执行
- 修改4个文件 + 删除1个文件 + 更新测试，50/50测试通过:
  1. dy/task_runner.py: 删 pipeline 导入和初始化, _interaction_enabled 简化为 key lookup, _interruptible_sleep 简化,
     _run_comment_lead_safely 返回单一 bool, _video_loop 互动区改为硬编码 if-elif 链 (like→follow+pm→video_comment→comment_lead),
     _selected_feature_chain_enabled 改为新字段
  2. dy/actions/commenting.py: ProcessCommentSectionAction.execute() 返回单一 bool, 删 _comment_made 追踪
  3. dy/config/user_settings.yaml: 删 enable_mode_* 和 pipeline_steps 字段
  4. dy/pipeline.py: 删除
  5. test_3features_analysis.py: 完全重写为硬编码链测试 (50项)
- 所有10个核心文件编译通过

## 已知遗留问题（待后续修复）
- backend/main.py, backend/schemas.py 仍引用旧配置键
- test_pipeline_chain.py 引用已删除的 pipeline.py

## Review + USB错误修复
- 审查重构改动: 无🔴问题, 2项🟡(comment_lead缺quota检查, _video_processing_timed_out冗余)
- frontend/douyin/douyin_app.py: USB检测加 ConnectionError/Timeout 分类提示
- frontend/douyin/douyin_app.py: _fetch_pipeline_steps 降级默认值 id "acquisition"→"author_follow" 对齐新架构
- 50/50测试通过
