# 授权码面板「已激活机器」积分增减与消耗倍率改造 — 计划书

> 服务器：`106.52.54.51` (root@)
> 站点：`https://lcjx.yun/workbench/ops-portal` → 授权码 标签页 → 已激活机器 列表
> 编制日期：2026-06-30
> 目标系统：SocialAutoAgent AI 积分服务 (credit_server)

---

## 一、现状调研结论

### 1.1 部署架构

```
浏览器
  │  https://lcjx.yun/workbench/ops-portal          (静态页 /www/wwwroot/lcjx.yun/static/ops-portal.html)
  │  https://lcjx.yun/social-ai-credit-api/admin/*   (前端 licenseApi 调用前缀)
  ▼
Nginx  (/www/server/panel/vhost/nginx/lcjx.yun.conf:205)
  │  location /social-ai-credit-api/  →  proxy_pass http://127.0.0.1:8020/api/
  ▼
credit_server  (FastAPI, /www/wwwroot/CloudSever.lcjx.yun/app/credit_server/server.py)
  │  uvicorn credit_server.server:app --host 127.0.0.1 --port 8020
  ▼
SQLite  /www/wwwroot/CloudSever.lcjx.yun/app/data/credit_server.db
```

### 1.2 关键文件清单

| 角色 | 路径 | 说明 |
|---|---|---|
| 运维门户页 | `/www/wwwroot/lcjx.yun/static/ops-portal.html` | 17.7KB，授权码面板在第 195-247 行 |
| 运维门户 JS | `/www/wwwroot/lcjx.yun/static/ops-console.js` | 32.5KB，`renderMachineTable` 在第 340 行 |
| 积分后端 | `/www/wwwroot/CloudSever.lcjx.yun/app/credit_server/server.py` | FastAPI 单文件，约 790 行 |
| 数据库 | `/www/wwwroot/CloudSever.lcjx.yun/app/data/credit_server.db` | SQLite，已有备份脚本 `backup_db.sh` |
| Nginx 反代 | `/www/server/panel/vhost/nginx/lcjx.yun.conf:205-206` | `/social-ai-credit-api/` → `127.0.0.1:8020/api/` |

### 1.3 当前数据模型（与本次需求相关）

**licenses**（授权码 / 积分账户）
```
license_key(PK)  customer_name  status  balance_credits  total_input_tokens
total_output_tokens  total_tokens  total_spent_credits  created_at  updated_at
```

**license_activations**（已激活机器，本次改造的核心表）
```
license_key + machine_id(PK)  device_id  hostname_hash  os_name  app_version
first_seen  last_seen  verify_count  last_ip  last_user_agent  status
```

**usage_logs**（消耗流水）已含 `machine_id`、`spent_credits`、`feature`、`success` 等字段。

**recharge_orders**（支付宝充值订单）。

### 1.4 当前积分 / 消耗逻辑（关键）

`server.py` 全局常量：

```python
TOKEN_PER_CREDIT = 1000          # 1 积分 = 1000 Token（全局固定）
MAX_MACHINES_PER_LICENSE = 3      # 单授权码最多绑 3 台
MIN_CREDITS_FOR_AI = 1.0          # 最低余额预检
```

消耗发生在 `spend_tokens()`（server.py:259），每次 AI 调用（`generate-video-comment` / `check-intent-comment` / `generate-lead-reply`）都执行：

```python
spent_credits = total_tokens / TOKEN_PER_CREDIT   # 全局统一，无机器维度倍率
# 原子扣减：UPDATE licenses SET balance_credits = balance_credits - ? ...
# 写流水：INSERT INTO usage_logs(..., machine_id, spent_credits, ...)
```

**`machine_id` 已在每次 AI 调用与 `spend_tokens` 中可用** —— 这是实现「按机器设倍率」的天然切入点。

### 1.5 当前「已激活机器」表格能力（ops-console.js:340 `renderMachineTable`）

仅展示，无任何编辑能力，列：`机器ID / 设备 / 系统 / 版本 / 首次验证 / 最近在线 / 次数·IP`。

### 1.6 当前充值 / 调整能力

- `POST /api/admin/licenses/{key}/recharge` —— 仅支持 `credits > 0`（`RechargeRequest: Field(gt=0)`），**无法扣减**，无操作原因，无审计。
- 没有 `PATCH` 机器、没有解绑、没有停用单台机器的接口。

### 1.7 数据规模（调研当时）

| 表 | 行数 |
|---|---|
| licenses | 1 |
| license_activations | 3 |
| usage_logs | 1705 |
| recharge_orders | 2 |

数据量小，迁移与改造零压力；但 `usage_logs` 增长快（已有 1705 条），需为后续审计做预留。

---

## 二、需求与目标

### 2.1 用户原始诉求

> 在 `https://lcjx.yun/workbench/ops-portal` 的「授权码」标签栏 →「已激活机器」列表中：
> 1. 可给用户**自由添加积分**（以及扣减）；
> 2. 可设置**消耗积分的倍率**；
> 3. 以企业级思路补齐必要的后台数据。

### 2.2 目标拆解（可验收）

| # | 目标 | 验收点 |
|---|---|---|
| G1 | 机器列表每行可调积分 | 行内「调整积分」按钮 → 输入金额(可正可负)+原因 → 余额实时变更，并写审计 |
| G2 | 机器列表每行可设倍率 | 行内倍率输入（默认 1.0）→ 保存 → 下次 AI 调用按新倍率扣费 |
| G3 | 倍率真实生效 | `spend_tokens` 按 `machine_id` 读取倍率并计入 `spent_credits` 与 `usage_logs` |
| G4 | 全程审计可追溯 | 积分调整、倍率变更均落审计表，含操作人/前后值/原因/IP |
| G5 | 零数据丢失 | 改造前后余额对账一致；老机器倍率默认 1.0，行为不变 |
| G6 | 企业级数据底座 | 流水分表、客户标签、预警、备份策略补齐（见第五章） |

---

## 三、技术方案

### 3.1 设计原则

- **积分仍归在 license 层**：现架构积分账户是 license，机器只是绑定关系。机器行「调整积分」= 调整该机器所属 license 的余额（UI 入口在机器行，语义对运营更直观，后端复用 license 账户，不引入双账户对账复杂度）。
- **倍率挂在机器层**：`machine_id` 已贯穿每次消耗，per-machine 倍率天然可行且语义清晰（同一客户不同设备可差异化计费）。
- **所有写操作审计化**：积分调整 / 倍率变更 / 解绑 全部进审计表。
- **向后兼容**：新增列带默认值，老数据零迁移；老接口不变。

### 3.2 数据库变更（DDL）

**A. `license_activations` 新增倍率列**（在 `ensure_columns()` 中做幂等迁移）

```sql
ALTER TABLE license_activations ADD COLUMN rate_multiplier REAL NOT NULL DEFAULT 1.0;
ALTER TABLE license_activations ADD COLUMN rate_updated_at TEXT NOT NULL DEFAULT '';
ALTER TABLE license_activations ADD COLUMN disabled INTEGER NOT NULL DEFAULT 0;  -- 单台机器停用开关
```

**B. 新增 `credit_adjustments` 审计表**（积分调整流水，区别于 usage_logs 的消耗流水）

```sql
CREATE TABLE IF NOT EXISTS credit_adjustments (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    license_key   TEXT NOT NULL,
    machine_id    TEXT NOT NULL DEFAULT '',
    amount        REAL NOT NULL,            -- 正=加，负=扣
    balance_before REAL NOT NULL,
    balance_after  REAL NOT NULL,
    reason        TEXT NOT NULL DEFAULT '',
    operator      TEXT NOT NULL DEFAULT 'admin',
    operator_ip   TEXT NOT NULL DEFAULT '',
    created_at    TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_credit_adj_license_time ON credit_adjustments(license_key, created_at);
```

**C. 新增 `admin_audit_logs` 通用审计表**（倍率变更、解绑、停用等非金额操作）

```sql
CREATE TABLE IF NOT EXISTS admin_audit_logs (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    action      TEXT NOT NULL,            -- set_multiplier / disable_machine / unbind_machine / ...
    target_type TEXT NOT NULL,           -- license / machine
    target_id   TEXT NOT NULL,
    before      TEXT NOT NULL DEFAULT '',
    after       TEXT NOT NULL DEFAULT '',
    operator    TEXT NOT NULL DEFAULT 'admin',
    operator_ip TEXT NOT NULL DEFAULT '',
    created_at  TEXT NOT NULL
);
CREATE INDEX IF NOT EXISTS idx_audit_action_time ON admin_audit_logs(action, created_at);
```

> 三张新表/列均带默认值，`init_db()` + `ensure_columns()` 幂等执行，老库直接平滑升级。

### 3.3 后端 API 变更（`credit_server/server.py`）

**A. 修改 `spend_tokens()` —— 引入 per-machine 倍率**

```python
def spend_tokens(license_key, input_tokens, output_tokens, feature, platform,
                 device_id, model, success, error="", machine_id=""):
    total_tokens = int(input_tokens or 0) + int(output_tokens or 0)
    # 新增：读取该机器倍率（默认 1.0）
    multiplier = get_machine_multiplier(license_key, machine_id)  # 新增辅助函数
    spent_credits = (total_tokens / TOKEN_PER_CREDIT) * multiplier if total_tokens > 0 else 0.0
    # ... 后续原子扣减、写 usage_logs 时把 multiplier 一并写入（usage_logs 新增列 rate_multiplier，便于事后核对）
```

同步给 `usage_logs` 增列（幂等迁移）：

```sql
ALTER TABLE usage_logs ADD COLUMN rate_multiplier REAL NOT NULL DEFAULT 1.0;
```

`get_machine_multiplier()`：

```python
def get_machine_multiplier(license_key, machine_id):
    with db() as conn:
        row = conn.execute(
            "SELECT rate_multiplier FROM license_activations WHERE license_key=? AND machine_id=?",
            (license_key, machine_id)).fetchone()
    return float(row["rate_multiplier"]) if row else 1.0
```

**B. 新增「调整积分」接口（支持正负 + 审计）**

```python
class AdjustRequest(BaseModel):
    amount: float           # 可正可负
    reason: str = Field("", max_length=200)
    machine_id: str = ""

@app.post("/api/admin/licenses/{license_key}/adjust")
def admin_adjust(license_key, req: AdjustRequest, request: Request, _: bool = Depends(require_admin)):
    if abs(req.amount) < 1e-9:
        raise HTTPException(400, "amount 不能为 0")
    ip = client_ip(request)
    ts = now_iso()
    with db() as conn:
        row = conn.execute("SELECT balance_credits FROM licenses WHERE license_key=?", (license_key,)).fetchone()
        if not row: raise HTTPException(404, "license not found")
        before = float(row["balance_credits"])
        after = before + req.amount
        if after < 0:
            raise HTTPException(400, f"扣减后余额为负({after:.3f})，拒绝")
        conn.execute("UPDATE licenses SET balance_credits=?, updated_at=? WHERE license_key=?", (after, ts, license_key))
        conn.execute("""INSERT INTO credit_adjustments
            (license_key, machine_id, amount, balance_before, balance_after, reason, operator, operator_ip, created_at)
            VALUES (?,?,?,?,?,?,?,?,?)""",
            (license_key, req.machine_id, req.amount, before, after, req.reason, "admin", ip, ts))
    return {"success": True, "balance_before": before, "balance_after": after}
```

**C. 新增「设置机器倍率 / 停用 / 解绑」接口**

```python
class MachineUpdateRequest(BaseModel):
    rate_multiplier: Optional[float] = None      # >0，None 表示不改
    disabled: Optional[int] = None                # 0/1

@app.patch("/api/admin/activations/{license_key}/{machine_id}")
def admin_update_machine(license_key, machine_id, req: MachineUpdateRequest, request, _: bool = Depends(require_admin)):
    # 校验倍率范围（如 0.1 ~ 10），写 admin_audit_logs，更新 rate_updated_at / status
    ...

@app.delete("/api/admin/activations/{license_key}/{machine_id}")
def admin_unbind_machine(license_key, machine_id, request, _: bool = Depends(require_admin)):
    # 软删：status='unbound'，写审计；保留历史
    ...
```

**D. 增强 `GET /api/admin/activations` 返回**（带客户名、余额、倍率）

```python
SELECT a.*, l.customer_name, l.balance_credits
FROM license_activations a
LEFT JOIN licenses l ON l.license_key = a.license_key
ORDER BY a.last_seen DESC LIMIT ?
```

**E. `register_activation()` 增加机器停用校验**：若 `disabled=1`，拒绝 AI 调用（403, "machine disabled"）。

### 3.4 前端变更（`ops-portal.html` + `ops-console.js`）

**A. `renderMachineTable()` 扩列**（ops-console.js:340 重写）

新列：`机器ID · 客户 · 授权码 · 剩余积分 · 消耗倍率 · 状态 · 操作`

```javascript
function renderMachineTable(activations = []) {
  return table(
    ["机器 / 客户", "授权码 / 余额", "倍率", "在线 / 次数", "状态", "操作"],
    (activations || []).map((row) => `
      <tr>
        <td><code>${esc(row.machine_id)}</code><br>
            <span class="ops-muted">${esc(row.customer_name||"-")} · ${esc(row.os_name||"")} ${esc(row.app_version||"")}</span></td>
        <td><code>${esc(row.license_key)}</code><br>
            <strong>${Number(row.balance_credits||0).toFixed(3)}</strong> 积分</td>
        <td><input class="ops-input" data-multiplier
                   data-license="${esc(row.license_key)}" data-machine="${esc(row.machine_id)}"
                   value="${Number(row.rate_multiplier||1).toFixed(2)}" style="width:72px">
            <button class="ops-btn secondary" data-set-multiplier>保存</button></td>
        <td>${esc(row.last_seen||"")}<br><span class="ops-muted">${esc(row.verify_count||0)} 次 · ${esc(row.last_ip||"")}</span></td>
        <td>${row.disabled ? '<span class="ops-badge bad">停用</span>' : '<span class="ops-badge">正常</span>'}</td>
        <td><div class="ops-actions">
            <button class="ops-btn secondary" data-adjust-credits
                    data-license="${esc(row.license_key)}" data-machine="${esc(row.machine_id)}">调整积分</button>
            <button class="ops-btn secondary" data-toggle-machine
                    data-license="${esc(row.license_key)}" data-machine="${esc(row.machine_id)}"
                    data-disabled="${row.disabled?1:0}">${row.disabled?'启用':'停用'}</button>
        </div></td>
      </tr>`)
  );
}
```

**B. 新增交互**

- 「调整积分」按钮 → 弹层：金额(可正负) + 原因(必填) → `POST /admin/licenses/{key}/adjust` → 刷新
- 「保存倍率」按钮 → `PATCH /admin/activations/{key}/{machine}` → 刷新
- 「停用/启用」→ `PATCH` 同接口 `disabled` 字段

**C. `ops-portal.html`**：在「已激活机器」区块（第 223 行）下方加一行说明文案「倍率 1.0 = 标准计费；调整积分会写入审计」。

### 3.5 安全与并发

- 所有新接口经 `require_admin`（已有 `ADMIN_TOKEN` + `hmac.compare_digest`）。
- `credit_adjustments` 写入与余额更新在同一 `db()` 事务内，避免对账漂移。
- 倍率范围服务端硬校验（建议 0.1 ≤ x ≤ 10），防前端绕过。
- 复用现有 `check_rate_limit` 限流。

---

## 四、企业级后台数据扩展（第六章详述）

除「积分增减 + 倍率」外，从企业级视角建议同步补齐（可分期）：

1. **积分全流水合一**：把 `recharge_orders`（充值）/ `usage_logs`（消耗）/ `credit_adjustments`（人工调整）三类来源用统一视图 `v_credit_ledger` 对账，财务可一键导出某客户某时段进出明细。
2. **客户标签与分组**：`licenses` 增 `tags`/`group`/`sales_owner`，支持按销售/渠道筛选与分成。
3. **倍率变更历史**：`admin_audit_logs` 已覆盖；建议加「到期时间」，支持限时促销倍率（如双 11 一周内 0.8 倍）。
4. **预警规则表**：低余额 / 异常高频 / 倍率突变，触发站内信或邮件。
5. **机器风控**：异常 IP 频次、跨地域登录、设备指纹变化 → 自动 disable。
6. **数据备份**：已有 `backup_db.sh`，建议补 cron 每日 + 滚动保留 14 份，并异机拷贝。
7. **DB 迁移工具**：当前靠 `ensure_columns` 手写 ALTER，建议引入轻量版本表 `schema_version` 做有序迁移。

---

## 五、实施步骤

> 全程在服务器 `106.52.54.51` 上操作，分 4 个阶段，每阶段独立可回滚。

### 阶段 0：备份与基线
1. `cp credit_server.db credit_server.db.bak-YYYYMMDD`；`cp server.py server.py.bak-...`；`cp ops-console.js/js.bak-...`；`cp ops-portal.html/.bak-...`
2. 导出现有 `licenses` / `license_activations` 余额快照（对账基线）。

### 阶段 1：后端（credit_server）
1. `init_db()` / `ensure_columns()` 内追加 3 张表 + 4 个新列的幂等 DDL。
2. 新增 `get_machine_multiplier()`，改 `spend_tokens()` 注入倍率。
3. 新增 `POST /api/admin/licenses/{key}/adjust`、`PATCH /api/admin/activations/{key}/{machine}`、`DELETE /api/admin/activations/{key}/{machine}`。
4. 增强 `GET /api/admin/activations` 联表返回。
5. 重启：`systemctl restart credit_server`（或对应 supervisor / pm2，按现网方式）。
6. 用 `curl + ADMIN_TOKEN` 自测 3 个新接口。

### 阶段 2：前端（ops-portal）
1. 重写 `renderMachineTable()` 扩列与操作按钮。
2. 新增「调整积分」弹层、「保存倍率」「停用/启用」事件。
3. `ops-portal.html` 补说明文案。
4. 浏览器硬刷新验证（注意清旧 JS 缓存）。

### 阶段 3：联调与对账
1. 调一笔 +10 积分 → 查 `credit_adjustments` 有记录、`licenses.balance_credits` +10。
2. 设某机器倍率 2.0 → 触发一次 AI → 查 `usage_logs.spent_credits` = 原值×2、`rate_multiplier=2.0`。
3. 调一笔扣减至负 → 应被 400 拒绝。
4. 停用机器 → AI 调用应 403。

### 阶段 4：企业级数据底座（可排期后续）
按第四章清单逐项落地，建议顺序：对账视图 → 备份 cron → 标签分组 → 预警 → 风控 → 迁移工具。

---

## 六、风险与回滚

| 风险 | 影响 | 缓解 / 回滚 |
|---|---|---|
| `spend_tokens` 改动引入计费错误 | 计费不准 | 倍率默认 1.0，老机器行为零变化；改动前备份 DB；改动后立即用一次 AI 调用对账 |
| 前端 JS 缓存导致旧表 | 运营看到旧表 | 文件名加版本号或硬刷新；保留 `.bak` 可秒回滚 |
| SQLite 并发写锁 | 高峰调整失败 | 现网量小(1705 行)，沿用单连接 `db()` 事务；后续可平滑迁 PostgreSQL |
| 倍率被设极端值(0/999) | 计费异常 | 服务端硬限 0.1~10；审计留痕 |
| 误扣积分无法恢复 | 客诉 | `credit_adjustments` 留前后值，可反向再调一笔恢复 |

回滚总开关：恢复 `server.py.bak` + `ops-console.js.bak` + `ops-portal.html.bak`，重启服务；DB 新增列/表对老代码无害（不读取即可），无需回滚 DB。

---

## 七、验收标准

- [ ] 机器列表每行显示：客户、授权码、余额、倍率（可编辑）、状态、操作按钮。
- [ ] 「调整积分」支持正负 + 原因，余额实时变更，`credit_adjustments` 有审计记录。
- [ ] 「保存倍率」生效后，下一次 AI 调用 `usage_logs.spent_credits` 按新倍率计算。
- [ ] 扣减后余额为负被拒绝；停用机器后 AI 调用被 403 拒绝。
- [ ] 老数据倍率默认 1.0，老授权码余额与改造前一致（对账无差）。
- [ ] 所有新接口需 `ADMIN_TOKEN` 鉴权；操作均记 IP、操作人、前后值。

---

## 八、附录：涉及文件与改动量预估

| 文件 | 改动 | 预估行数 |
|---|---|---|
| `credit_server/server.py` | DDL + 3 新接口 + `spend_tokens` 改造 + 联表查询 | +约 120 行 |
| `static/ops-console.js` | `renderMachineTable` 重写 + 3 个事件 | +约 80 行 |
| `static/ops-portal.html` | 说明文案 + 弹层结构 | +约 25 行 |
| `credit_server.db` | 3 新表 / 4 新列（幂等迁移） | 0 数据丢失 |

> 注：本计划书仅为方案，未对生产环境做任何代码改动。待您确认后，我再按「阶段 0→3」逐阶段在服务器上实施，每阶段完成即向您回报。
