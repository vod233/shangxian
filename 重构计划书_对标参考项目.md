# 重构计划书：对标参考项目流程重构

## 一、两个项目的核心差异对照

| 维度 | 参考项目 (D:\CodingTest\111\main\main) | 当前项目 (D:\CodingTest\111test\111) | 本次改动 |
|------|------|------|------|
| **互动编排** | 硬编码 if-elif 顺序执行 | 声明式管道 `PipelineStep` + `PipelineExecutor` | **改为硬编码** |
| **文件引入** | 无 `pipeline.py` | 有 `dy/pipeline.py` (336行) | **删除 pipeline.py** |
| **功能开关** | `enable_like`, `enable_author_follow`, `enable_video_comment`, `enable_comment_lead`, `enable_private_message` | 新模式字段 `enable_mode_customer_acquisition`, `enable_mode_content_interaction` + 旧字段 | **改为参考项目的简单 bool 开关** |
| **开关判定** | `_interaction_enabled(key, default=True)` → 简单 key lookup | 同上函数但含复杂向后兼容推断 | **简化为 key lookup** |
| **赞判断** | `_interaction_enabled("enable_like")` | 独立步骤 `like: enabled_key="enable_like"` | **并入 if-elif** |
| **视频评论** | `_interaction_enabled("enable_video_comment")` | `video_comment: enabled_key="enable_mode_content_interaction"` | **改为独立开关** |
| **评论区截流** | `_interaction_enabled("enable_comment_lead")` | `comment_lead: enabled_key="enable_mode_content_interaction"`, self_managed | **改为独立开关** |
| **私域获客** | `enable_author_follow` + `enable_private_message` 两个独立开关 | `acquisition: enabled_key="enable_mode_customer_acquisition"` 合并开关 | **拆回两个独立开关** |
| **skip_remaining** | `skip_remaining_features` 标志位 | `blocked` + `block_rest` 机制 | **改为标志位** |
| **步骤间隔** | 每个功能后 `random.uniform()` | 管道内统一 1.75s 固定 | **改为每个功能后独立间隔** |
| **截流返回值** | `_run_comment_lead_safely` → bool | → (recovered, comment_made) | **改为 bool** |
| **ProcessCommentSection** | `execute()` 返回 `_close_comment_section()` bool | 返回 `(closed_ok, comment_made)` 元组 | **改为 bool** |
| **限额记录** | task_runner 中直接调 `db.update_interaction` | `PipelineExecutor._should_record_quota()` | **改为直接调用** |
| **状态上报** | 无 | `status_reporter` 回调 | **保留** |
| **网络检测** | 无 | `_check_network()` | **保留** |
| **概率决策** | `anti.should_interact('long_watch')` | `_probability_enabled()` + `_should_interact()` | **保留** |
| **config 格式** | `enable_like: false` 等 | 含 `enable_mode_*` 新模式字段 | **改为参考项目格式** |
| **随机/确定性** | 全部 random | 测试期全部固定 | **保留用户已测试的确定性** |

---

## 二、具体改动清单（按文件，按优先级）

### 改动 1：`dy/task_runner.py` — 核心改造

这是本次重构唯一需要大幅修改的文件。

#### 1.1 导入修改

```python
# 删除
from .pipeline import PipelineExecutor, PIPELINE_STEPS

# 新增（已有，保留）
from .actions.interaction import (
    DoubleClickLikeAction, FollowAuthorAction, GetCurrentVideoLinkAction
)
```

#### 1.2 `__init__` 修改

```python
# 删除
self.pipeline = PipelineExecutor(self)
self.status_reporter = status_reporter

# 保留
self.is_stopped = False
self.is_paused = False
self.current_keyword = None
```

#### 1.3 `_interaction_enabled` 修改

```python
# 改为参考项目的简单实现
def _interaction_enabled(self, key, default=True):
    return bool(self.config.get('interaction', {}).get(key, default))
```

#### 1.4 `_interruptible_sleep` 修改

```python
# 恢复参考项目的简单实现
def _interruptible_sleep(self, seconds):
    slept = 0
    while slept < seconds:
        self._check_stop()
        time.sleep(0.5)
        slept += 0.5
```

#### 1.5 `_run_comment_lead_safely` 修改

```python
# 返回类型从 (bool, bool) 改为 bool
def _run_comment_lead_safely(self, video_title, keyword):
    ...
    # 删除 comment_made 相关
    # ProcessCommentSectionAction 返回 bool
    return recovered  # 单一 bool
```

#### 1.6 `_video_loop` 完全重写

核心改动：**用参考项目的硬编码 if-elif 链替换 pipeline.run() 调用**

参考项目的逻辑区域 (行 536-642):

```python
# B.1 点赞
if self._interaction_enabled("enable_like"):
    self._check_stop()
    if self.anti.can_do('like'):
        stable, liked = self._run_feature_safely(
            "点赞",
            lambda: self.runner.run_action(DoubleClickLikeAction),
        )
        if liked:
            self.db.update_interaction(video_id, "like")
        if not stable:
            skip_remaining_features = True
    else:
        logger.info("限额决策: 跳过点赞")
    self._interruptible_sleep(random.uniform(0.8, 2.0))
else:
    logger.info("已关闭功能: 点赞，跳过")

# B.2 关注+私信
if self._interaction_enabled("enable_author_follow"):
    ...
    self._interruptible_sleep(random.uniform(1.0, 2.5))
else:
    logger.info("已关闭功能: 作者主页/关注/私信，跳过")

# B.3 AI视频评论
if self._interaction_enabled("enable_video_comment"):
    ...
else:
    logger.info("已关闭功能: AI 视频评论，跳过")

# B.4 评论区截流
if self._interaction_enabled("enable_comment_lead"):
    ...
else:
    logger.info("已关闭功能: 评论区 AI 截流/楼中楼回复，跳过")
```

### 改动 2：`dy/actions/commenting.py` — 最小化改动

只改 `ProcessCommentSectionAction.execute()` 返回值：

```python
# 改前: return self._close_comment_section(), self._comment_made
# 改后: return self._close_comment_section()
```

删除 `self._comment_made` 相关代码，不再追踪是否发了评论。

### 改动 3：`dy/actions/interaction.py` — 无需改动

参考项目和当前项目的 `interaction.py` 功能一致，只是点击方式不同（参考用 `node.click()/d.click()`，当前用 `safe_node_click/human_click`）。**保留当前的改法**。

### 改动 4：`dy/pipeline.py` — 删除

```bash
# 整个文件删除
rm dy/pipeline.py
```

### 改动 5：`dy/config/user_settings.yaml` — 配置格式对齐

```yaml
# 改为参考项目格式
interaction:
  enable_like: false          # 点赞
  enable_author_follow: true  # 关注作者
  enable_private_message: false # 私信
  enable_video_comment: true  # AI视频评论
  enable_comment_lead: true   # 评论区截流
  min_followers_threshold: 0.01
  pm_followers_threshold: 0.01
  pm_message_list: ["话术1", "话术2"]
  max_comment_swipes: 2
  max_ai_comment_reviews: 20
  intent_keywords: []
```

### 改动 6：`dy/actions/base_action.py` — 无需改动

保留 `safe_node_click` 方法。

### 改动 7：`dy/anti_detection.py` — 无需改动

保留我们之前的所有修复（fast_tap 三段式、HumanSleep 确定性等）。

### 改动 8：`dy/ai_reply_agent.py` — 无需改动

保留我们之前的扩展（30+意向词、智能截断等）。

### 改动 9：测试文件 — `test_3features_analysis.py`

需要更新测试以匹配新的硬编码流量逻辑（不再使用 pipeline）。

---

## 三、不改动的文件（零改动）

| 文件 | 原因 |
|------|------|
| `dy/actions/navigation.py` | 双方一致，无需改动 |
| `dy/main_controller.py` | 双方一致，无需改动 |
| `dy/db_manager.py` | 双方一致，无需改动 |
| `dy/actions/locators.py` | 双方一致，无需改动 |
| `dy/core/device_mgr.py` | 双方一致，无需改动 |
| `dy/core/app_mgr.py` | 双方一致，无需改动 |
| `dy/logger_config.py` | 双方一致，无需改动 |
| `backend/*` | 不在本次重构范围 |
| `frontend/*` | 不在本次重构范围 |

---

## 四、执行步骤（按顺序，不可逆）

| 步骤 | 文件 | 操作 | 预估行数变化 |
|------|------|------|-------------|
| **S1** | `dy/task_runner.py` | 删除 pipeline 导入 + `self.pipeline` 初始化 | -3行 |
| **S2** | `dy/task_runner.py` | 简化为 `_interaction_enabled` | -20行 |
| **S3** | `dy/task_runner.py` | 改 `_interruptible_sleep` | -2行 |
| **S4** | `dy/task_runner.py` | 改 `_run_comment_lead_safely` → 返回 bool | -5行 |
| **S5** | `dy/task_runner.py` | 重写 `_video_loop` 中的互动区 → 硬编码链 | -40行 / +70行 |
| **S6** | `dy/actions/commenting.py` | `ProcessCommentSectionAction` 返回单一 bool | -3行 |
| **S7** | `dy/pipeline.py` | 删除整个文件 | -336行 |
| **S8** | `dy/config/user_settings.yaml` | 配置格式对齐 | -5行 / +5行 |
| **S9** | `test_3features_analysis.py` | 更新测试匹配新逻辑 | ~50行 |

**总计**: 约减 400+ 行代码，净增 ~70 行。

---

## 五、风险与验证

| 风险 | 等级 | 应对 |
|------|------|------|
| config 格式变更导致前端不兼容 | 🟡 | 前端需同步改为上新字段 |
| 管道删除后测试文件报错 | 🟡 | 更新测试 |
| skip_remaining 逻辑覆盖不完整 | 🟡 | 与参考项目一一对照确认 |
| 评论区截流返回值变更 | 🟢 | 仅影响 task_runner 中的判断 |

---

## 六、验证方法

1. `py_compile` 全部修改文件
2. `python test_3features_analysis.py` 跑通 52 项测试
3. 真机跑一遍全功能链条确认正常运行

---

## 七、不改动的我们已有的改进（优势保留）

以下是我们比参考项目做得更好的地方，全部保留：

- `safe_node_click()` 代替裸 `node.click()` — 避免长按弹窗
- `fast_tap` 三段式 down→move→up — 彻底消除长按判定
- `_send_text_from_focused_input` 三级验证链 — 确保评论真正发送
- `_input_is_cleared` — 发送后清空检测
- `_should_record_quota` 精准配额逻辑 — 已融入硬编码链
- `_dismiss_video_context_menu_if_present` 在 `_run_feature_safely` 后调用
- `finally: touch.up(0,0)` 僵尸触控清理
- 30+ 意向识别关键词
- 智能截断 (sanitize_reply)
- 确定性测试模式 (HumanSleep/InteractionProbability 固定值)
