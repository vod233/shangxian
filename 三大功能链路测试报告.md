# 三大功能链路测试报告

> 测试日期：2026-06-27  
> 测试范围：私域获客 / AI视频评论 / 评论区截流 — 单独链路 & 自由组合  
> 测试方法：代码静态审查 + 52项单元测试（50通过 / 2失败，失败项揭示了代码问题）  
> 测试原则：**仅发现问题并记录，不修改代码**

---

## 一、测试概览

| 测试维度 | 测试场景数 | 通过 | 失败 | 发现代码问题 |
|---------|-----------|------|------|------------|
| 私域获客-单独 | 7 | 7 | 0 | 3 |
| AI视频评论-单独 | 7 | 7 | 0 | 2 |
| 评论区截流-单独 | 5 | 5 | 0 | 2 |
| 自由组合(7种) | 10 | 9 | 1 | 2 |
| 边界条件 | 5 | 5 | 0 | 1 |
| AI代理逻辑 | 10 | 9 | 1 | 2 |
| 评论区操作 | 3 | 3 | 0 | 0 |
| 互动操作 | 2 | 2 | 0 | 0 |
| **合计** | **52** | **50** | **2** | **12** |

---

## 二、P0 级问题（严重，会影响核心功能正确性）

### P0-1: 评论区截流双重状态恢复包裹

**问题文件**: `dy/task_runner.py` + `dy/pipeline.py`  
**影响范围**: 评论区截流（comment_lead）

**问题描述**:
`_step_comment_lead` 的执行路径存在三重状态恢复嵌套：

```
PipelineExecutor.run()
  └─ _run_feature_safely("评论区截流", ...)       ← 第1层：恢复到视频页
       └─ _step_comment_lead(flow, ctx)
            └─ flow._run_comment_lead_safely(...)   ← 第2层：再次恢复到视频页
                 ├─ _recover_to_video_page(...)     ← 重复检查
                 ├─ OpenCommentSectionAction         ← 打开评论区
                 ├─ ProcessCommentSectionAction      ← 处理评论区
                 ├─ _recover_to_video_page(...)      ← 第2层恢复
                 └─ return (recovered, comment_made)
            └─ return (recovered, comment_made)
       └─ _recover_to_video_page(...)               ← 第3层恢复
```

**后果**:
1. 每次执行评论区截流，至少多做2次恢复检查（打开前+关闭后各多一次）
2. 第2层（`_run_comment_lead_safely`）恢复失败返回 `recovered=False`，但第3层（外层 `_run_feature_safely`）可能会成功恢复，导致 `stable=True`，使 `on_failure="block_rest"` 的阻断策略**失效**
3. 测试验证：comment_lead 恢复失败后 `blocked` 仍为 `False`（参见测试 FAIL-2）

**建议修复方向**: `_step_comment_lead` 不应在 pipeline executor 的 `_run_feature_safely` 外层包裹中被再次恢复——要么让 pipeline executor 识别这是自恢复步骤并跳过包裹，要么移除 `_run_comment_lead_safely` 的内部恢复逻辑。

---

### P0-2: 评论配额在未实际发送时仍被消耗

**问题文件**: `dy/pipeline.py` 第95-97行  
**影响范围**: AI视频评论 + 评论区截流

**问题描述**:
```python
# 记录限额
if result and step.quota_keys:   # result 是回调的返回值
    for q in step.quota_keys:
        self.flow.anti.record_action(q)
```

对于 `comment_lead` 步骤，回调返回 `(recovered: bool, comment_made: bool)`：
- 如果 `recovered=True, comment_made=False`（恢复成功但未找到意向评论），result = `(True, False)` 这个**元组是真值**，于是 `record_action("comment")` 被调用，配额被消耗，但实际上并没有发送任何评论。
- 这会导致 commentary 配额（每日50条）被虚耗，影响后续真正有效的评论。

**同样的问题存在于 `_step_video_comment`**:
- 回调返回 `(stable: bool, posted: bool)`。如果 `stable=True, posted=False`，result = `(True, False)` 也是真值，配额同样被记录。

**建议修复方向**: `quota recording` 应检查实际动作是否执行成功，而非仅检查回调返回值是否为真。

---

### P0-3: 本地意向识别正则缺少常见关键词

**问题文件**: `dy/ai_reply_agent.py` 第388行  
**影响范围**: AI禁用时或云端AI调用失败时的评论区截流意向识别

**问题描述**:
```python
default_pattern = r"(怎么买|哪里|求|想要|了解|价格|多少|推荐|教程|方法|链接|资料|怎么做|靠谱吗|有用吗)"
```

缺少常见意向表达词如：
- `好用吗`（测试验证 FAIL-1：`is_intent_comment("好用吗")` 返回 `False`）
- `有效吗`、`真的吗`、`怎么样`
- `在哪`、`多少钱`、`贵吗`

**建议修复方向**: 补充缺失的常见意向关键词。

---

## 三、P1 级问题（重要，影响用户体验或逻辑健壮性）

### P1-1: 私域获客阻断策略过于激进

**问题文件**: `dy/pipeline.py` 第208行  
**影响范围**: 私域获客 + AI视频评论/评论区截流组合

**问题描述**:
私域获客步骤设置 `on_failure="block_rest"`，意味着当关注/私信失败时（包括页面跳转失败、粉丝阈值不满足等），AI视频评论和评论区截流也被跳过。

但实际上：
- 用户可能不在乎关注成功与否，更在乎评论引流
- `FollowAuthorAction` 内部已有粉丝阈值过滤，第132-136行会把 `returned_to_video` 标记为 `True`，但这个返回逻辑并不稳定

**测试验证**:
- 场景5（私域获客+评论区截流）：私域获客失败确实阻断了后续的评论区截流（确认正确实现了设计意图）

**建议修复方向**: 评估是否将 `on_failure` 改为 `"continue"`，或改为可配置的阻断策略。

---

### P1-2: _interaction_enabled 向后兼容推断引发错误激活

**问题文件**: `dy/task_runner.py` 第77-89行  
**影响范围**: 模式开关的向后兼容推断

**问题描述**:
```python
if key in ("enable_mode_content_interaction",):
    if "enable_mode_content_interaction" in interaction:
        return bool(interaction["enable_mode_content_interaction"])
    return bool(interaction.get("enable_video_comment", True)) or bool(interaction.get("enable_comment_lead", True))
```

当用户只想开 video_comment 但不需要 comment_lead 时：
- 旧配置：`enable_video_comment: true, enable_comment_lead: false`
- 新字段 `enable_mode_content_interaction` 不存在时 → 返回 `True or False = True`
- 导致 `enable_mode_content_interaction=true` → video_comment 和 comment_lead **都激活**

即使 pipeline_steps 层面能控住，但如果 `pipeline_steps.comment_lead` 未设置（默认 None），会退化到 mode-level 检查，结果是 comment_lead 被意外开启。

**建议修复方向**: 向后兼容推断应更保守，或默认在 migration 阶段强制要求新字段。

---

### P1-3: _step_acquisition stable 返回值语义不一致

**问题文件**: `dy/pipeline.py` 第129-146行  
**影响范围**: 私域获客阻断策略的可靠性

**问题描述**:
```python
if isinstance(result, dict):
    return result.get("returned_to_video", True), result
# 向后兼容：旧版 FollowAuthorAction 返回 True
if result:
    flow.db.update_interaction(ctx["video_id"], "follow")
return True, result
```

- 新版返回 `dict` 时，`stable` 由 `returned_to_video` 控制 → 正确
- 旧版返回 `True` 时，`stable` **始终为 `True`** → 即使实际上没有恢复成功

当 `FollowAuthorAction` 在某些路径下返回非 dict 的 `True` 但实际上页面已偏离视频页时，阻断策略会静默失效。

**建议修复方向**: 统一 `FollowAuthorAction.execute()` 返回值格式，始终返回 dict；或提高 `_step_acquisition` 对返回值类型的判断覆盖。

---

### P1-4: AI评论生成空值仍然写入数据库

**问题文件**: `dy/pipeline.py` 第149-161行  
**影响范围**: AI视频评论的数据记录

**问题描述**:
```python
comment_text = flow.reply_agent.generate_reply(...) or ""
if not comment_text:
    flow.db.save_ai_reply(ctx["video_id"], note_title=ctx["video_title"], ai_reply="")
    return True, False
flow.db.save_ai_reply(ctx["video_id"], note_title=ctx["video_title"], ai_reply=comment_text)
```

当 AI 返回空字符串时，仍然调用 `save_ai_reply` 保存空值。如果降级模板也失败，数据库中会留下大量 `ai_reply=""` 的记录。

**建议修复方向**: 空值时不应写入数据库，或至少标记为 `_failed` 状态。

---

### P1-5: _run_comment_lead_safely 返回值解构冗余

**问题文件**: `dy/task_runner.py` 第388-389行  
**影响范围**: 评论区截流的结果传递

**问题描述**:
```python
if isinstance(result, (tuple, list)) and len(result) >= 2:
    _, comment_made = result[0], result[1]
```

这里的 `_` 占位符实际没有被使用（它原本是 `closed_ok`），但 `ProcessCommentSectionAction.execute()` 返回的 `(closed_ok, comment_made)` 中 `closed_ok` 可能提供有意义的信息（评论区是否成功关闭），却被丢弃了。这导致外层调用者无法区分"评论区关闭失败"和"未找到意向评论"两个不同的失败场景。

**建议修复方向**: 保留 `closed_ok` 返回给调用者，或在日志中记录。

---

## 四、P2 级问题（一般，但可能引起边缘场景异常）

### P2-1: 步骤间间隔计算逻辑不准确

**问题文件**: `dy/pipeline.py` 第108行  
**影响范围**: 管道执行间隔

**问题描述**:
```python
if len(executed) < len([s for s in steps if self.flow._interaction_enabled(s.enabled_key)]):
    self._interruptible_sleep(random.uniform(1.0, 2.5))
```

计数逻辑统计所有 enabled 步骤数，但 `acquisition` 和 `video_comment/comment_lead` 属于不同的 enabled_key，组合模式下计数可能偏高或偏低，导致最后一步执行完后仍然多等一个间隔。

**建议修复方向**: 维护剩余待执行步骤的准确计数。

---

### P2-2: 配额检查 for-else 语义可能导致并发问题

**问题文件**: `dy/pipeline.py` 第78-81行  
**影响范围**: 多设备共享配额时的数据一致性

**问题描述**:
```python
for q in step.quota_keys:
    if not self.flow.anti.can_do(q):
        logger.info(f"跳过 {step.label}: {q} 限额已达")
        break
else:
    # 概率
    ...
    # 执行...
    # 记录限额
    if result and step.quota_keys:
        for q in step.quota_keys:
            self.flow.anti.record_action(q)
```

在 `can_do` 检查和 `record_action` 之间存在时间窗口：
- 如果 quota 剩余1条，两个设备同时检查 `can_do("comment")` → 都返回 True
- 两个设备都执行→ 都记录 → 实际超限

由于 `DailyLimitManager` 使用内存计数而非原子操作，多设备场景下此问题存在。

**建议修复方向**: 添加配额检查与记录之间的锁机制。

---

### P2-3: 本地意向识别短评文本异常

**问题文件**: `dy/ai_reply_agent.py` 第77行  
**影响范围**: 评论区截流意向识别

**问题描述**:
```python
if not clean_comment or len(clean_comment) < 3:
    return False
```

与 `_is_reviewable_comment` 中的 `len(clean) < 4` 阈值不一致：
- `is_intent_comment` 过滤 `< 3` 字的评论
- `_is_reviewable_comment` 过滤 `< 4` 字的评论

导致 3 字评论可能通过 `is_intent_comment` 的入口检测但被 `_is_reviewable_comment` 过滤掉，浪费一次 AI 调用。

**建议修复方向**: 统一两处的长度阈值。

---

## 五、自由组合测试结论

### 组合测试矩阵

| 私域获客 | AI视频评论 | 评论区截流 | 测试结果 | 问题 |
|---------|-----------|-----------|---------|------|
| ✅ 开   | ❌ 关     | ❌ 关     | ✅ 通过 | 正常：like → acquisition |
| ❌ 关   | ✅ 开     | ❌ 关     | ✅ 通过 | 正常：like → video_comment |
| ❌ 关   | ❌ 关     | ✅ 开     | ✅ 通过 | 正常：like → comment_lead |
| ✅ 开   | ✅ 开     | ❌ 关     | ✅ 通过 | 正常：like → acq → vc |
| ✅ 开   | ❌ 关     | ✅ 开     | ✅ 通过 | 正常：like → acq → cl |
| ❌ 关   | ✅ 开     | ✅ 开     | ✅ 通过 | 正常：like → vc → cl |
| ✅ 开   | ✅ 开     | ✅ 开     | ✅ 通过 | 正常：like → acq → vc → cl |
| acq失败 | ✅ 开     | ✅ 开     | ⚠️ 阻断 | acq失败 → 后续全阻断 (符合设计) |
| 任何   | 配额耗尽  | 配额耗尽  | ✅ 通过 | video_comment 和 comment_lead 都跳过，共享 comment 配额正确 |

### 组合场景关键发现

1. **阻断链验证正确**：acquisition 失败 → block_rest → video_comment + comment_lead 均被跳过（设计意图如此）
2. **共享配额正确**：video_comment 和 comment_lead 共享 `comment` 配额，任一耗尽两者都跳过
3. **pipeline_steps 粒度开关优先于 mode-level**：per-step 配置覆盖 mode-level（正确）
4. **步骤执行顺序不可变**：like → acquisition → video_comment → comment_lead（固定顺序）
5. **comment_lead 恢复失败阻断失效**：见 P0-1

---

## 六、AI 评论代理（DYReplyAgent）问题汇总

| # | 问题 | 严重度 | 位置 |
|---|------|--------|------|
| 1 | `is_intent_comment` 的 `clean_comment` 长度阈值（<3）与 `_is_reviewable_comment`（<4）不一致 | P2 | `ai_reply_agent.py:77` vs `commenting.py:325` |
| 2 | 本地意向识别正则缺少 "好用吗" / "有效吗" 等常见词 | P0 | `ai_reply_agent.py:388` |
| 3 | `_build_lead_fallback` 话术过于模板化，连续使用易于被识别为机器 | P2 | `ai_reply_agent.py:408-413` |
| 4 | `generate_reply` 的 `_sanitize_reply` 截断到38字符可能切除中文最后一个字 | P2 | `ai_reply_agent.py:337` |
| 5 | `_BANNED_PATTERNS` 中 "主页" 出现在评论过滤中，但 `_build_lead_fallback` 生成的话术包含 "主页" | P2 | `ai_reply_agent.py:32` vs `:408-413` |

> **注意**: P2-5 的 "主页" 同时出现在禁止词和兜底话术中。禁止词用于视频评论 (`_is_valid_reply`)，允许 "主页" 的兜底话术用于楼中楼回复 (`_is_valid_lead_reply`)。两者是不同的过滤规则，目前没有冲突，但如果未来合并规则需要注意。

---

## 七、当前配置状态分析

基于 `dy/config/user_settings.yaml` 的当前配置：

```yaml
interaction:
  enable_mode_customer_acquisition: false   # 私域获客 → 关闭
  enable_mode_content_interaction: true     # 内容互动模式 → 开启
  pipeline_steps:
    like: true           # 点赞 → 开
    acquisition: false   # 私域获客 → 关
    video_comment: false # AI视频评论 → 关
    comment_lead: true   # 评论区截流 → 开
```

**当前实际执行流程**: 点赞 → (跳过acquisition) → (跳过video_comment) → 评论区截流

这是"仅评论区截流"模式，与组合测试场景3一致，测试通过。

---

## 八、测试代码

完整测试文件位于：`test_3features_analysis.py`（52项测试用例）

运行方式：
```bash
python test_3features_analysis.py
```

---

## 九、总结

| 严重度 | 数量 | 说明 |
|--------|------|------|
| P0 | 3 | 双重恢复包裹 / 配额虚耗 / 意向识别遗漏 |
| P1 | 5 | 阻断策略过激 / 向后兼容推断 / 返回值语义 / 数据库脏写 / 解构冗余 |
| P2 | 5 | 间隔计算 / 配额竞态 / 阈值不一致 / 话术模板化 / 截断问题 |

**本次测试确认了设计架构的整体正确性**（管道编排、开关体系、限额管理均按预期工作），发现的问题主要集中在边界条件、深度嵌套的恢复逻辑缺陷以及某些异步竞态场景下的数据一致性问题。所有问题均已记录，等待后续修复排期。
